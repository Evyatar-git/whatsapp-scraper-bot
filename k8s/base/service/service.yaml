apiVersion: v1
kind: Service
metadata:
  name: whatsapp-scraper-service
  namespace: whatsapp-scraper
  labels:
    app: whatsapp-scraper
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: whatsapp-scraper